config {
  type: "table",
  schema: "marts",
  name: "bridge_user_friend"
}

SELECT
  u.user_id,
  friend_id
FROM ${ref("int_user_features")} u,
UNNEST(SPLIT(u.friends_raw, ',')) AS friend_id
WHERE u.friends_raw IS NOT NULL